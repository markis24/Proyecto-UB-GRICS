import React from 'react';
import ReactDOM from 'react-dom/client';
import Form from './Componentes/Formulario Contacto/Form.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <Form/>
    </React.StrictMode>,
);
